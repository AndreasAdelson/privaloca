{% extends 'base.html.twig' %}

{% block title %}{{ 'subscription_title_show_page'|trans }}{% endblock %}

{% block body %}
    <show-subscription
    :subscription-id="{{ subscriptionId }}"
    :can-edit="{{ canEdit|json_encode }}"
    url-precedente="{{ urlPrecedente }}"
    ></show-subscription>
{% endblock %}
